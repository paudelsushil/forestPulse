% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_files.R
\name{extract_and_rename_tifs}
\alias{extract_and_rename_tifs}
\title{Batch Extract and Rename TIFs from ZIP files}
\usage{
extract_and_rename_tifs(
  zip_dir,
  output_dir,
  file_to_extract = "odm_orthophoto/odm_orthophoto.tif"
)
}
\arguments{
\item{zip_dir}{Character scalar. Path to the folder containing the
\code{.zip} files.}

\item{output_dir}{Character scalar. Path to the folder where extracted
\code{.tif} files are written. Created if it does not exist.}

\item{file_to_extract}{Character scalar. Path of the file to extract from
within each zip, e.g. \code{"odm_orthophoto/odm_orthophoto.tif"}.}
}
\value{
Invisibly \code{NULL}; called for its side effect of extracting and
renaming files.
}
\description{
Iterates through all \code{.zip} files in a directory, extracts a single
target \code{.tif} from each, renames that file to match its parent zip, and
writes it to one output directory. Errors on individual zips are caught and
reported as warnings so the batch continues.
}
\examples{
# Not run: operates on external .zip archives produced by drone
# photogrammetry software (e.g. OpenDroneMap) and relies on an external
# zip/unzip utility, so it cannot be executed in a self-contained example.
\dontrun{
extract_and_rename_tifs(
  zip_dir    = "data/zips",
  output_dir = file.path(tempdir(), "orthophotos")
)
}

}
