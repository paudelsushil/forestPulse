% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_timeclass.R
\name{create_timeclass}
\alias{create_timeclass}
\title{Assign Rows to Consecutive Time Classes}
\usage{
create_timeclass(data, date_column, start_date, end_date, by, types)
}
\arguments{
\item{data}{A data frame (or tibble) containing the time column.}

\item{date_column}{Character scalar. Name of the column in \code{data} that
holds the numeric time values (for example a column of years).}

\item{start_date}{Numeric scalar. Start of the first window (inclusive).}

\item{end_date}{Numeric scalar. Upper bound used to generate windows; the
last window ends at or before this value.}

\item{by}{Numeric scalar. Width of each window in the units of
\code{date_column} (for example \code{5} for 5-year classes).}

\item{types}{Character scalar. Prefix used to build the name of the new
column, which is \code{paste0(types, "By", by, "yrs")}.}
}
\value{
\code{data} with one additional character column giving the time
class of each row (formatted \code{"start-end"}), or \code{NA} for rows
whose value falls outside every window.
}
\description{
Bins the values of a numeric time column (e.g. calendar years) into
consecutive, non-overlapping windows of fixed width and labels each row with
the window it falls into. Useful for grouping observations into periods such
as 5-year or decadal classes prior to summarising.
}
\examples{
df <- data.frame(year = c(1986, 1991, 2003, 2018))
create_timeclass(df, date_column = "year",
                 start_date = 1985, end_date = 2020,
                 by = 5, types = "fire")

}
