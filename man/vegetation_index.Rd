% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vegetation_indices.R
\name{vegetation_index}
\alias{vegetation_index}
\title{Calculate Vegetation Indices from an RGB Orthomosaic Image}
\usage{
vegetation_index(
  index_name,
  image_path = NULL,
  red_band = 1,
  green_band = 2,
  blue_band = 3,
  output_path = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{index_name}{Character string. The abbreviation of the vegetation index
(case-insensitive). Use "list" to see all supported indices.}

\item{image_path}{Character string or SpatRaster. File path to a raster
image (e.g., GeoTIFF orthomosaic), or an already-loaded SpatRaster object.}

\item{red_band}{Integer or character. Band number or band name for the Red
channel (default = 1).}

\item{green_band}{Integer or character. Band number or band name for the
Green channel (default = 2).}

\item{blue_band}{Integer or character. Band number or band name for the Blue
channel (default = 3).}

\item{output_path}{Character string or NULL. If provided, the result raster
is saved to this path. If NULL (default), a path is auto-generated in the
same directory as the input image.}

\item{overwrite}{Logical. Overwrite existing output file? Default FALSE.}
}
\value{
A \code{SpatRaster} object containing the computed vegetation index.
}
\description{
Computes a specified vegetation index from an RGB orthomosaic raster image.
Automatically detects Red, Green, and Blue bands by name when available.
Supports 37 RGB-based vegetation indices.
}
\examples{
# List all available indices
vegetation_index("list")

}
