% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_vegetation.R
\name{impute_plots_to_pixels}
\alias{impute_plots_to_pixels}
\title{Impute Reference Plots to Target Pixels}
\usage{
impute_plots_to_pixels(
  reference,
  target,
  predictors,
  responses,
  k = 1,
  ntree = 500,
  chunk_size = 1e+05,
  seed = 42
)
}
\arguments{
\item{reference}{Data frame of reference plots with a \code{plot_id} column,
the predictor columns, and the response columns.}

\item{target}{Data frame of target pixels with \code{pixel_id},
\code{stand_id}, and the same predictor columns as \code{reference}.}

\item{predictors}{Character vector of predictor column names, present in
\emph{both} \code{reference} and \code{target} (the matching variables).}

\item{responses}{Character vector of response (forest-attribute) column
names used to grow the random forest; required in \code{reference} only.}

\item{k}{Integer. Number of neighbours to assign (\code{1} = single
best-matching plot). Default \code{1}.}

\item{ntree}{Integer. Number of trees in the random forest. Default
\code{500}.}

\item{chunk_size}{Integer. Number of target pixels imputed per chunk.
Default \code{1e5}.}

\item{seed}{Integer. Random seed for reproducibility. Default \code{42}.}
}
\value{
A long data frame with columns \code{pixel_id}, \code{stand_id},
\code{neighbor} (1..\code{k}), \code{plot_id}, and \code{distance}.
}
\description{
Assigns each target pixel its best-matching reference plot(s) using
random-forest nearest-neighbour imputation (\code{yaImpute::yai}). Imputation
is performed in chunks so that millions of pixels can be processed without
exhausting memory.
}
\examples{
\dontrun{
# reference plots: predictors + responses + plot_id
reference <- read.csv("plots_reference.csv")
#   plot_id, elev, slope, aspect, tmean, precip,       <- predictors
#            canopy_ht, forest_cover, veg_group        <- responses

# target rasters (SAME predictors), plus the stand grid
preds  <- terra::rast("predictors.tif")   # layers named as predictors
stands <- terra::rast("gis/stand_grid.tif")
target <- make_target_from_rasters(preds, stands)

predictors <- c("elev", "slope", "aspect", "tmean", "precip")
responses  <- c("canopy_ht", "forest_cover", "veg_group")

pixel_plot <- impute_plots_to_pixels(reference, target,
                                     predictors, responses, k = 1)

# tree_lists from the FIA->cohort conversion (previous step), long format
tree_lists <- read.csv("plot_tree_lists.csv")
init <- build_iland_init(pixel_plot, tree_lists, "init/init_trees.txt")
}

}
