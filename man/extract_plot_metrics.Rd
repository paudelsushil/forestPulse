% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_plot_metrics.R
\name{extract_plot_metrics}
\alias{extract_plot_metrics}
\title{Extract Gap Metrics Matched to Field Plot Footprints}
\usage{
extract_plot_metrics(
  chm,
  plots,
  plot_id = "plot_id",
  x_col = "x",
  y_col = "y",
  plot_crs = "EPSG:4326",
  radius = 15.24,
  gap_threshold = 3,
  threshold_method = "fixed",
  min_gap_area = 1,
  multi_radius = NULL,
  save_clips = FALSE,
  clip_dir = NULL
)
}
\arguments{
\item{chm}{SpatRaster or file path. Canopy Height Model.}

\item{plots}{sf object, data.frame, or file path. Plot center locations.}

\item{plot_id}{Character. Column name for plot identifiers.
Default \code{"plot_id"}.}

\item{x_col, y_col}{Coordinate columns if \code{plots} is a data.frame.}

\item{plot_crs}{CRS for data.frame coordinates. Default \code{"EPSG:4326"}.}

\item{radius}{Numeric. Plot radius in meters. Default \code{15.24}
(50 ft USFS standard).}

\item{gap_threshold}{Numeric. CHM height below which pixels are
classified as gap (meters). Default \code{3}.}

\item{threshold_method}{Character. \code{"fixed"} or \code{"otsu"}.
Default \code{"fixed"}.}

\item{min_gap_area}{Numeric. Minimum gap patch size in m2.
Default \code{1}.}

\item{multi_radius}{Numeric vector. Optional additional radii for
sensitivity analysis. e.g. \code{c(25, 50, 75)}.
Default \code{NULL} (primary radius only).}

\item{save_clips}{Logical. Save clipped binary rasters per plot?
Default \code{FALSE}.}

\item{clip_dir}{Directory for saved clips, required when
\code{save_clips = TRUE}. Default \code{NULL} (nothing written). Supply a
path such as \code{file.path(tempdir(), "gap_clips")} to save clips.}
}
\value{
A \code{data.frame} with one row per plot (per radius if
\code{multi_radius} is used) containing plot ID, radius, and
all gap metrics.
}
\description{
Clips a CHM (or binary gap raster) to each field plot's circular
footprint and computes gap geometry metrics within that footprint.
This ensures spatial correspondence between remotely sensed gap
metrics and field-measured variables (regeneration, fuel loads,
ground vegetation).
}
\examples{
# synthesise a CHM and field plot centres (no CRS for a self-contained
# example; in practice both inputs should carry a projected CRS in metres)
set.seed(1)
chm <- terra::rast(
  nrows = 60, ncols = 60,
  xmin = 0, xmax = 60, ymin = 0, ymax = 60,
  vals = runif(3600, 0, 25)
)

plots <- sf::st_as_sf(
  data.frame(plot_id = c("P1", "P2"), x = c(20, 40), y = c(30, 30)),
  coords = c("x", "y")
)

metrics <- extract_plot_metrics(
  chm           = chm,
  plots         = plots,
  radius        = 10,
  gap_threshold = 3
)

}
