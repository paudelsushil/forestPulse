% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iland_preparation.R
\name{create_stand_grid}
\alias{create_stand_grid}
\title{Create an iLand stand grid from stand polygons}
\usage{
create_stand_grid(
  polygons,
  id_field,
  resolution = 10,
  surrounding_forest = NULL,
  outside_code = NA_integer_,
  forested_outside_code = -2L,
  nodata = -9999L,
  buffer = 0,
  filename = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{polygons}{An \code{sf} polygon layer of stands.}

\item{id_field}{Name of the column holding integer stand IDs (> 0).}

\item{resolution}{Grid resolution in CRS units (default 10).}

\item{surrounding_forest}{Optional \code{sf} of forest outside the project area;
these cells receive \code{forested_outside_code}.}

\item{outside_code}{Value for non-forested outside cells. Use \code{NA} to rely on
the grid NODATA value (iLand treats NODATA as non-forested). Default \code{NA}.}

\item{forested_outside_code}{Value for forested-outside cells (default -2).}

\item{nodata}{NODATA flag written to the file (default -9999).}

\item{buffer}{Optional buffer (CRS units) added around the extent (default 0).}

\item{filename}{Optional output path (\code{.asc}/\code{.txt}). If \code{NULL}, nothing is written.}

\item{overwrite}{Overwrite an existing file (default \code{FALSE}).}
}
\value{
A \code{terra::SpatRaster} (returned invisibly when a file is written).
}
\description{
Rasterizes stand polygons to the iLand stand grid, encoding forested
stands (integer IDs > 0), forested-outside cells, and non-forested cells.
}
\examples{
# two stand polygons with integer IDs
sq <- function(x0, y0, s, id) {
  sf::st_sf(stand_id = id,
            geometry = sf::st_sfc(sf::st_polygon(list(rbind(
              c(x0, y0), c(x0 + s, y0), c(x0 + s, y0 + s),
              c(x0, y0 + s), c(x0, y0))))))
}
stands <- rbind(sq(0, 0, 50, 1L), sq(50, 0, 50, 2L))

# in-memory grid (no file written)
g <- create_stand_grid(stands, id_field = "stand_id", resolution = 10)

# write to a temporary file
f <- tempfile(fileext = ".asc")
create_stand_grid(stands, id_field = "stand_id", filename = f,
                  overwrite = TRUE)
file.remove(f)
}
