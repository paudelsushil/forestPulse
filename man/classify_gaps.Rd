% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gap_metrics.R
\name{classify_gaps}
\alias{classify_gaps}
\title{Classify a CHM or RGB Raster into Binary Canopy/Gap}
\usage{
classify_gaps(
  r,
  threshold = NULL,
  method = "fixed",
  min_area = 0,
  output = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{r}{SpatRaster or file path. CHM, single-band index, or DN image.}

\item{threshold}{Numeric. Pixels below this value are classified as gap.
For CHM: typical value 2-5 m. For DN/index: depends on your data.}

\item{method}{Character. Thresholding method:
\code{"fixed"} (default) uses the supplied threshold value.
\code{"otsu"} computes the threshold automatically.}

\item{min_area}{Numeric. Minimum gap area in square meters. Gaps smaller
than this are removed. Default \code{0} (keep all).}

\item{output}{Optional file path to save the binary raster.}

\item{overwrite}{Logical. Default \code{FALSE}.}
}
\value{
A binary SpatRaster (gap = 1, canopy = 0).
}
\description{
Creates a binary raster where gap = 1 and canopy = 0 using a height
or brightness threshold.
}
\examples{
# synthesise a small CHM
set.seed(1)
chm <- terra::rast(nrows = 30, ncols = 30, vals = runif(900, 0, 25))

# fixed threshold
gaps <- classify_gaps(chm, threshold = 3)

# automatic Otsu threshold
gaps_otsu <- classify_gaps(chm, method = "otsu")

}
