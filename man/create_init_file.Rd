% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iland_preparation.R
\name{create_init_file}
\alias{create_init_file}
\title{Create an iLand initialization file (standgrid mode)}
\usage{
create_init_file(
  trees,
  cohorts = TRUE,
  cols = list(),
  bin_width = 5,
  units = c("metric", "imperial"),
  hd_default = 80,
  species_set = NULL,
  stand_grid = NULL,
  sep = " ",
  filename = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{trees}{A \code{data.frame}/\code{data.table} of either cohorts (with
\verb{count, species, dbh_from, dbh_to, hd, stand_id}) or raw tree records.}

\item{cohorts}{Logical; if \code{FALSE}, \code{trees} is raw records to be binned. Default \code{TRUE}.}

\item{cols}{Named list mapping roles to column names in \code{trees} when \code{cohorts = FALSE},
e.g. \code{list(dbh = "DIA", height = "HT", count = "TPA", species = "code", stand = "stand_id")}.}

\item{bin_width}{DBH class width (CRS/metric units) when binning raw records (default 5).}

\item{units}{One of \code{"metric"} or \code{"imperial"}; if \code{"imperial"}, converts inches->cm,
feet->m, trees/acre->trees/ha. Default \code{"metric"}.}

\item{hd_default}{Fallback height/diameter ratio when height is missing (default 80).}

\item{species_set}{Optional character vector of allowed iLand species codes;
rows with other codes raise an error.}

\item{stand_grid}{Optional \code{terra::SpatRaster} stand grid OR an \code{sf}/list spec passed
to \code{\link[=create_stand_grid]{create_stand_grid()}}; used to validate \code{stand_id}.}

\item{sep}{Output field separator (default " ").}

\item{filename}{Optional output path.}

\item{overwrite}{Overwrite existing file.}
}
\value{
A \code{data.frame} with columns
\verb{count, species, dbh_from, dbh_to, hd, age, density, stand_id}.
}
\description{
Produces a validated standgrid-mode init file from either ready cohort rows or
raw tree records (optionally binned into DBH classes and unit-converted), and
optionally cross-checks \code{stand_id} against a stand grid.
}
