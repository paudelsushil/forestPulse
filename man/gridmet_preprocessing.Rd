% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_climate_database.R
\name{gridmet_preprocessing}
\alias{gridmet_preprocessing}
\title{Preprocess gridMET daily data into an iLand climate table}
\usage{
gridmet_preprocessing(
  data,
  cluster_col,
  date_col,
  bands = list(tmmn = "tmmn", tmmx = "tmmx", pr = "pr", srad = "srad", rmin = "rmin",
    rmax = "rmax", vpd = "vpd"),
  temp_unit = c("K", "C"),
  rad_unit = "W/m2",
  vpd_source = c("gridmet", "compute"),
  svp_method = c("FAO56", "Murray"),
  co2 = NULL
)
}
\arguments{
\item{data}{A \code{data.frame}/\code{data.table} of daily gridMET values already
extracted to climate clusters (e.g. via \code{terra}/\code{climateR}).}

\item{cluster_col}{Name of the column identifying the climate cluster/table.}

\item{date_col}{Name of a \code{Date} column (used to derive year/month/day).}

\item{bands}{Named list mapping gridMET variables to columns in \code{data}.
Recognised names: \code{tmmn}, \code{tmmx}, \code{pr}, \code{srad}, \code{rmin}, \code{rmax}, \code{vpd}.
gridMET native names are the defaults.}

\item{temp_unit}{Unit of \code{tmmn}/\code{tmmx} in \code{data}: \code{"K"} (converted to C) or \code{"C"}.}

\item{rad_unit}{Unit of \code{srad} in \code{data} (passed to \code{\link[=convert_radiation]{convert_radiation()}}).}

\item{vpd_source}{\code{"gridmet"} to pass through the native \code{vpd} band, or
\code{"compute"} to derive VPD from temperature and RH via \code{\link[=calc_vpd]{calc_vpd()}}.}

\item{svp_method}{Saturation-vapour-pressure method when computing VPD.}

\item{co2}{Optional CO2: \code{NULL}, a single ppm value, or a \code{data.frame(year, co2)}.}
}
\value{
A \code{data.table} ready for \code{write_iland_climate(split_col = cluster_col)}.
}
\description{
Converts an extracted gridMET daily table (one row per cluster-day) into the
harmonized \code{data.table} consumed by \code{\link[=write_iland_climate]{write_iland_climate()}}: columns
\verb{year, month, day, min_temp, max_temp, prec, rad, vpd} (+ optional \code{co2}),
alongside the cluster column.
}
\examples{
\dontrun{
clim <- gridmet_preprocessing(
  data        = gridmet_daily,
  cluster_col = "cluster",
  date_col    = "date",
  temp_unit   = "K",
  vpd_source  = "compute",
  co2         = data.frame(year = 1979:2020, co2 = co2_ppm))
write_iland_climate(clim, "database/gridmet.sqlite", split_col = "cluster",
                     overwrite = TRUE)
}
}
\references{
Abatzoglou, J.T. (2013). Development of gridded surface meteorological data
for ecological applications and modelling. International Journal of
Climatology, 33(1), 121-131. (gridMET / METDATA)
}
